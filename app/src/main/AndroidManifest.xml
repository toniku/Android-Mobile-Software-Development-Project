<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hokikoutsi2019">

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/koutsi_launcher"
        android:label="@string/default_app_name"
        android:roundIcon="@mipmap/koutsi_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NewGameActivity" />
        <activity android:name=".GameReportActivity" />
        <activity android:name=".LatestGamesActivity" />
        <activity android:name=".LineupActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".PlayerCardActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".LineEditActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>